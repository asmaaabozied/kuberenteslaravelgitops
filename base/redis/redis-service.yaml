apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: default
  labels:
    app: redis
spec:
  type: ClusterIP   # داخلي فقط - الافتراضي
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app: redis
